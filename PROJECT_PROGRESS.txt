==============================================================
WAVEX - AI DECISION OPERATING SYSTEM FOR STARTUP FOUNDERS
Project Progress Log (Hackathon)
==============================================================

WHAT WAVEX IS
--------------------------------------------------------------
Not a chatbot. A decision operating system: founders ask business
questions, specialized AI experts analyze them using the company's
own data, a Judge issues a structured recommendation, and every
decision is remembered forever. The system gets smarter about the
company with every decision logged.

ARCHITECTURE - THE THREE BRAINS
--------------------------------------------------------------
Brain 1 - Business Context (SQLite via SQLAlchemy, Postgres-ready)
  - companies, documents, decisions tables
  - decisions table = the Decision Ledger (permanent audit trail)
  - Data survives restarts; swap to PostgreSQL = one env var

Brain 2 - Business Intelligence (RAG on ChromaDB)
  - Uploaded docs (PDF/TXT/MD) -> chunked (250 words, 40 overlap)
    -> embedded locally (all-MiniLM-L6-v2, no API key) -> indexed
  - Metadata filtering: company_id (privacy), domain (routing)
  - Global knowledge bases seeded at startup:
      * Legal KB: 9 chunks (India-focused: incorporation, ESOP,
        DPDP/GDPR, employment, contracts, fundraising, IP, compliance)
      * Finance KB: 4 chunks (runway/burn benchmarks, hiring costs,
        unit economics, fundraising norms)

Brain 3 - Decision Intelligence (Groq LLM agents)
  - Planner: selects ONLY relevant experts per question
    (keyword fallback if LLM unavailable)
  - Experts:
      * FinanceAgent  - KNOWLEDGE agent, grounded in finance KB
      * LegalAgent    - KNOWLEDGE agent, grounded in legal KB
      * HiringAgent   - ACTION agent, produces action_output
        (job descriptions, hiring plans)
      * GTMAgent      - ACTION agent, produces action_output
        (GTM plans, outreach sequences)
  - Judge: synthesizes into recommendation / why / trade-offs /
    next steps / confidence
  - Every agent degrades gracefully to fallback output if the
    LLM is unavailable (demo never crashes)

KEY FEATURES BUILT
--------------------------------------------------------------
1. Company profiles + document upload into private knowledge base
2. RAG-grounded expert analysis (agents cite the founder's own
   documents and the knowledge bases by name)
3. Specific agent selection (Planner routes legal questions to
   Legal only, hiring+budget to Hiring+Finance, etc.)
4. Action agents produce ready-to-use deliverables (action_output)
5. Decision Ledger: every decision persisted with full analysis
6. DECISION MEMORY with re-evaluation:
   - Approved/rejected decisions indexed into vector memory
   - New similar question -> response includes similar_past_decision
     (decision id, title, status, days_ago, similarity score)
   - Judge explicitly re-evaluates in light of the past decision
     and time elapsed
7. Database firewall:
   - API_KEY env -> all data routes require X-API-Key header (401
     otherwise); /health reports firewall status
   - CORS locked to whitelisted origins
   - ORM-parameterized queries (SQL injection blocked)
   - Multi-tenant isolation (company A can never retrieve
     company B's data - tested)

API ENDPOINTS
--------------------------------------------------------------
POST /company                          create company
POST /company/{id}/documents           upload doc into knowledge base
GET  /company/{id}/documents           list documents
POST /decision                         run full decision pipeline
GET  /decision/{id}                    fetch one decision
GET  /history                          decision ledger
POST /approve                          approve/reject (+ index into memory)
GET  /health                           health + firewall status

TECH STACK
--------------------------------------------------------------
FastAPI + Pydantic | SQLAlchemy + SQLite (PostgreSQL-ready)
ChromaDB (embedded vector DB) | all-MiniLM-L6-v2 local embeddings
Groq (llama-3.3-70b-versatile) for agent inference
pypdf for PDF extraction | React frontend (separate branch)

HOW TO RUN
--------------------------------------------------------------
cd backend
python -m pip install -r requirements.txt
copy .env.example .env   (add GROQ_API_KEY; optional API_KEY
                          to enable the firewall)
python -m uvicorn app.main:app --reload
Open http://127.0.0.1:8000/docs
NOTE: .env changes require a manual server restart.
First document upload downloads the embedding model once (~80MB).

TESTING DONE
--------------------------------------------------------------
- 9-check end-to-end suite: CRUD, ingestion, retrieval, privacy
  isolation, ledger persistence across restarts
- 8-check feature suite: firewall 401s, KB seeding, planner
  selection, KB retrieval, action_output flow
- 5-check decision memory suite: memory hit on similar question,
  rejected-decision recall, cross-company isolation, no false
  positives on unrelated questions
- Live verification with real Groq calls: planner selected
  ['LegalAgent'] for a legal question; experts cited the uploaded
  financial plan ($400k budget, 14-month runway) and knowledge
  bases by name; Finance and Hiring agents genuinely disagreed

WHY NOT JUST USE CLAUDE/CHATGPT ($20 ARGUMENT)
--------------------------------------------------------------
A chat LLM is a brilliant consultant with amnesia. WaveX is a
system of record: (1) Decision Ledger - auditable history of every
recommendation and approval; (2) organizational memory - answers
grounded in YOUR documents and YOUR past decisions, improving over
time; (3) structured output - recommendation/why/trade-offs/next
steps/confidence, every time; (4) multi-agent disagreement surfaces
trade-offs a single model blends away. The LLM is swappable;
the memory and workflow are the product.

PENDING / NEXT
--------------------------------------------------------------
- Frontend: render similar_past_decision banner, action_output
  cards, document upload UI, font fix
- Merge all branches into main
- Optional: Supabase PostgreSQL swap (one env line)
- Optional: hosting/deployment (Dockerfile exists)
- Optional: conflict-detection/debate round between experts

==============================================================
ONBOARDING FLOW (added)
==============================================================
Sign Up with Google (demo auth, real-OAuth-swappable) ->
first-timers: 2-step onboarding | returning: straight to dashboard.

Step 1 Founder form: role, experience, country, timezone, language,
decision style, biggest challenge, notification preference.
Step 2 Company form: name, industry, stage, team size, revenue,
burn, cash, funding raised/stage, RUNWAY AUTO-CALCULATED live,
multi-select business goals, Company Context Memory textarea.

DB tables (FK-linked, Postgres-ready): founders, founder_preferences,
companies (extended with metrics), company_goals, context_memory.
SQLite auto-migration adds new columns to existing databases.

ADVISOR CONTEXT INJECTION: every POST /decision builds a
COMPANY BUSINESS PROFILE block (revenue, burn, runway, team, stage,
funding, goals, founder style/challenge) and injects it into every
expert prompt + the Judge automatically. Context Memory is chunked
into ChromaDB so advisors also retrieve it semantically. The founder
NEVER repeats their details.

New endpoints: POST /auth/google, POST /onboarding/founder,
POST /onboarding/company, GET /founder/{id}/summary.
Dashboard greets: "Good Evening, Akshay" + company/stage/runway/
revenue/goal stats, live from the summary API.

Verified: 7-check e2e suite (auth routing, runway calc 11.1mo,
returning-user redirect, dashboard summary, agent prompts provably
contain the injected profile, context memory retrievable).

==============================================================
JWT AUTHENTICATION (added - security branch, 2026-07-05)
==============================================================
Production-grade auth layer on top of the existing firewall.

Signup/Login: POST /api/auth/signup (email + password >= 8 chars,
bcrypt-hashed, 409 on duplicate, case-insensitive email) and
POST /api/auth/login (OAuth2 password form: username=email) ->
HS256 JWT with user id in `sub`, expiry from settings
(SECRET_KEY / ALGORITHM / ACCESS_TOKEN_EXPIRE_MINUTES=30 in .env).

DB: new `users` table (id, unique email, hashed_password,
created_at); `companies.user_id` FK (SET NULL, indexed) added to
the SQLite auto-migration map. Legacy rows keep user_id NULL.

Route protection: get_current_user dependency (app/api/deps.py)
decodes the Bearer token, validates signature + expiry, resolves
`sub` -> users row; ONE standardized 401 for every failure mode
(no leak of why). Applied to ALL data routes; each is tenancy-
scoped to the caller's own companies/decisions/documents.
Cross-tenant access returns 404 (no ID probing possible).
Ownership gate runs BEFORE any agent/RAG pipeline executes.

New endpoints (existing paths unchanged - frontend safe):
GET  /company/{id}              fetch own company
PUT  /company/{id}              update own company
POST /documents/upload          flat upload (company_id form field)
GET  /documents                 all docs across own companies
POST /decision/{id}/approve     approve by path id
POST /decision/{id}/reject      reject by path id

Firewall interplay: /api/auth/signup + /api/auth/login added to
PUBLIC_PATHS so an armed firewall never blocks the login door.
Legacy /auth/google onboarding flow left public (demo auth).

New deps: python-jose[cryptography], passlib[bcrypt],
bcrypt==4.0.1 (passlib 1.7.4 compat pin), email-validator.

TESTED: 20-check smoke suite, all passing - signup 201/409/422,
login 200, wrong-password + unknown-user return IDENTICAL 401s
(no user enumeration), missing/garbage/forged/expired/no-sub/
nonexistent-user tokens all 401, owner reads own company 200,
cross-tenant read 404.

PENDING: frontend must call login and attach Authorization:
Bearer <token> on all data requests (dashboard 401s until then).
