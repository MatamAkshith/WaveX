# WaveX Backend

This is the FastAPI backend application for the WaveX platform, serving as the core API and agent service layer. It provides structured endpoints, agent workflow management, and RAG integration.

## Running the Application

To run the application locally, navigate to the `backend` directory, install the dependencies, and run:

```bash
uvicorn app.main:app --reload
```
